<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RideSharing" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/green</item>
        <item name="colorPrimaryVariant">@color/lavender</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/green</item>
        <!-- Button style -->
        <item name="buttonStyle">@style/MyButtonStyle</item>
        <!-- App bar style -->
        <item name="android:actionBarStyle">@style/MyAppBarStyle</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="MyButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">@color/buttonColor</item> <!-- Button background color -->
        <item name="android:textColor">@android:color/white</item> <!-- Button text color -->
    </style>

    <style name="MyAppBarStyle" parent="Widget.MaterialComponents.Toolbar">
        <item name="android:background">@color/appBarColor</item> <!-- App bar color -->
        <item name="android:titleTextColor">@android:color/white</item> <!-- App bar title text color -->
    </style>
</resources>
